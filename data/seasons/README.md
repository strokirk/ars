# Downtime metadata (`data/seasons/*.yaml`)

Hand-maintained, one file per book — same shape and same reason as
[`data/guidelines/README.md`](../guidelines/README.md): a supplement drops in
another `*.yaml` with its own `book:` header, no code change, no registry. An
entry whose `key` matches a core one **replaces** it for that book; a new
`key` is added alongside. Loaded by `web/src/lib/seasons.ts`.

## Schema

```yaml
book:
  key: core
  name: Ars Magica — Definitive Edition
  abbr: Core

totals:               # a named sum — Lab Total, Teaching Total, a book's Quality…
  - key: lab
    name: Lab Total
    terms:
      - {kind: art, role: technique}   # the activity says which Art (or fixedArts)
      - {kind: art, role: form}
      - {kind: characteristic, of: Int}
      - {kind: ability, of: Magic Theory}
      - {kind: aura}
      - {kind: fixed, value: 3}         # a flat constant
      - {kind: input, label: "...", default: 0}  # the player types a number

activities:
  - key: invent-spell
    name: Invent a spell
    category: lab                       # lab | write | study | teach
    total: lab                          # which total this reads (omit for no number)
    fixedArts: {technique: Creo, form: Vim}  # omit to let the player pick Technique/Form
    work: {kind: accumulate, unit: "points", subtractGoal: true}
    needsGoal: true
    goalLabel: "Spell level"
    sideTotals: [{key: age, label: "Vis cost"}]  # extra read-only numbers shown alongside
    summary: One line for the row.
    detail: Full prose when expanded.
```

### Term kinds (closed set — six)

`characteristic` · `ability` · `art` · `aura` · `fixed` · `input`. A
supplement that only adds activities on existing totals is pure data; one
with genuinely new arithmetic needs a new term or work kind — a deliberate
code change, not a config language.

### Work kinds (closed set — five)

| kind | shape |
|---|---|
| `repeatable` | `floor`/`ceil`(total ÷ `divisor`) per season, forever — vis extraction |
| `xp` | the total *is* the xp gained this season — study, teaching, adventure |
| `fixed` | one season, a fixed result — writing a tractatus, a Longevity Ritual |
| `accumulate` | needs a `goal`; rate is `total` (or `total − goal` with `subtractGoal`) per season until points reach the goal — inventing a spell, writing a summa |
| `charges` | needs a `goal`; blocked below it, `floor((total − goal) / 5) + 1` charges otherwise |

## What's deliberately not modelled

A few real formulas need a number this engine has no way to derive on its
own (the Shape & Material bonus of a specific item, the level-drop Quality
bonus on a summa, how many tractatus you've already written this lifetime).
Rather than guess, the affected activity's `detail` says so and points at
the browser's ad-hoc "+ Add adjustment" row instead. Extend the engine (a
new term/work kind) only when a second real activity needs the same shape —
see `docs/DOWNTIME-PLAN.todo.md`.

## Editing

Not regenerated by `tools/build.sh` — these are hand-written, like
`data/guidelines/*.yaml`. `cd web && pnpm test` covers the loader.
